/**
 * Deprecated: Spotify helpers based on import.meta.env have been removed.
 *
 * Use the runtime-aware helpers and API routes instead:
 * - Env access: src/lib/runtime-env.ts (locals.runtime.env)
 * - Access token: src/lib/spotify.ts (getAccessToken)
 * - API routes: src/pages/api/spotify/*.ts
 */

export {};
